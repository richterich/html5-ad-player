{
  "name": "html5-ad-player",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint --config .eslintrc.json \"**/*.js\"",
    "lint:fix": "eslint --config .eslintrc.json \"**/*.js\" --fix",
    "lint:staged": "lint-staged",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.25.0",
    "eslint-config-standard": "^17.0.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "vite": "^3.1.0"
  }
}
